@model IEnumerable<SmartClinic.Models.QRKod>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VrijednostKoda)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DatumGenerisanja)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Termin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VrijednostKoda)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DatumGenerisanja)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Termin.Vrijeme)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.QRKodId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.QRKodId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.QRKodId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
